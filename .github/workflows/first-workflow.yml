name: "workflow_dispatch Example"
on:
  workflow_dispatch:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greetings
        run: echo "Hello welcome to Gitaction"
      - name: Print Bye
        run: echo "Bye !!"
