name: schedule example
on:
  schedule:
    - cron: '1 * * * *'
jobs:
  Build:   
    runs-on: ubuntu-latest
    steps:
      - name: "Schedule job example"
        run: echo "Schedule job is triggered !!!"
    